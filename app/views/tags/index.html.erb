<style>
  h1 {
    border-bottom: solid 3px gray;
    margin-bottom: 24px;
  }
</style>

  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.title, topic_url(tag.id), :method=> 'get' %>
    </li>
  <% end %>

</ul>
